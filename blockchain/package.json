{
  "scripts": {
    "compile": "set \"APPDATA=.\\.appdata\" && set \"LOCALAPPDATA=.\\.localappdata\" && hardhat compile",
    "deploy": "set \"APPDATA=.\\.appdata\" && set \"LOCALAPPDATA=.\\.localappdata\" && hardhat run scripts/deploy.js --network sepolia",
    "deploy:local": "set \"APPDATA=.\\.appdata\" && set \"LOCALAPPDATA=.\\.localappdata\" && hardhat run scripts/deploy.js",
    "interact": "set \"APPDATA=.\\.appdata\" && set \"LOCALAPPDATA=.\\.localappdata\" && hardhat run scripts/interact.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2"

  }
}
